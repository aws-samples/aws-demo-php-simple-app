
php:
  version: '5.6'

tests:
  - type: phpunit
    mode: parallel
    output: exit-status
    command: vendor/bin/solano-phpunit
    config: blog-phpunit.xml    # Defaults : phpunit.xml, phpunit.xml.dist
    files:
      - tests/**Test.php
